
# IDE temporary files (add more as needed)
/.vscode
# Lib crates so dependency versions are not locked
Cargo.lock
# Compiled targets and binaries (this prevents from 
# having any src dir with these names)
**/target
# These are backup files generated by rustfmt
**/*.rs.bk
